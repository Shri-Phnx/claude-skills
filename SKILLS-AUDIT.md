# Skills & Plugins Audit

Tracks what's actually installed in a live Claude session versus what's checked into this repo, plus any external dependency each skill/plugin needs to actually work. Generated by comparing the session's live skill manifest against `skills/*/SKILL.md`.

Last updated: 2026-09-03

---

## 1. Installed but NOT in this repo

These skill names are active in the session with no matching folder under `skills/`.

1. `content-gold-miner-public`
2. `script-prep-public`
3. `video-editing-public`
4. `video-import-public`
5. `watch`

> Update 2026-09-04: all five of the above were added to the repo. See §4 for details — leaving this list as-is as a record of the original audit.

**Not actually missing — Claude Code's own built-in skills**, never meant to live in this repo: `session-start-hook`, `doc-coauthoring`, `web-artifacts-builder`, `theme-factory`, `internal-comms`, `brand-guidelines`, `slack-gif-creator`, `algorithmic-art`, `xlsx`, `pptx`, `pdf`, `docx`, `design`, `dataviz`, `artifact-design`, `artifact-diagramming`, `artifact-capabilities`, `update-config`, `keybindings-help`, `code-review`, `simplify`, `fewer-permission-prompts`, `loop`, `claude-api`, `run`, `init`, `security-review`, `import-memory`, `learn`, `morning`.

**Possibly the same skill under an older name** — repo folder uses a shorter name than the currently-installed skill; worth confirming before renaming:

| Installed as | Repo folder |
|---|---|
| `social-content` | `social` |
| `signup-flow-cro` | `signup` |
| `schema-markup` | `schema` |
| `referral-program` | `referrals` |
| `product-marketing-context` | `product-marketing` |
| `popup-cro` | `popups` |
| `paywall-upgrade-cro` | `paywalls` |
| `pricing-strategy` | `pricing` |
| `onboarding-cro` | `onboarding` |
| `rtcfrr-prompt` | `rtcfrr-prompt-builder` |

---

## 2. Plugins — 100% installed and active

The session's skill manifest tags each skill with a `source`. Only the ones tagged `"source": "plugin"` (each with its own `backingPluginId`) are backed by a real, successfully-installed plugin — meaning the plugin synced cleanly and the plugin's skill is live in this session. Everything else is either a Claude Code built-in (`anthropic` / `anthropic-example`) or one of your own hand-built skills (`custom`) with no plugin behind it.

| Skill (plugin-backed) | Backing plugin ID | In this repo? | Dependency |
|---|---|---|---|
| `watch` | `plugin_01FYaT54eZSb9nWN5sBJSm8y` | No | `yt-dlp` (download), `ffmpeg` (frame extraction), video captions or a Whisper API fallback for transcript |
| `content-gold-miner-public` | `plugin_018oaWKPEWqRtF7JBDVEqfkC` | No | A Claude Project with your creator profile (audience/voice/goal) set up once and reused |
| `video-editing-public` | `plugin_0191dqJSvd1QGfEFnNraxH3S` | No | Descript MCP connector; a **paid** Descript plan with AI credits (flagged as the most credit-hungry skill of the set); Claude Code or Cowork for the local-upload step |
| `video-import-public` | `plugin_01LzSmys6Wykj89qjpyHr2vi` | No | A Descript account (destination); local file access via Claude Code/Cowork, or a hosted URL when on claude.ai Chat |
| `script-prep-public` | `plugin_017fkDowtHxBQYJg8EmAWZdf` | No | Same Claude Project creator profile as `content-gold-miner-public` |
| `auto-review-loop` | `plugin_011HCKLMrnUXipLQ6a7YzNnL` | Yes — `skills/aris-auto-review-loop/` | An external reviewer backend: Codex, or a manual reviewer |
| `karpathy-guidelines` | `plugin_012Es8c1xdXiiPXxhsGkvX9f` | Yes — `skills/karpathy-guidelines/` | None — pure guidelines, no external tool |
| `arxiv` | `plugin_01RYLZnQ93vNo8ZrBv53hdzZ` | Yes — `skills/aris-arxiv/` | arXiv API / internet access; a local paper-library folder to save into |

The `watch` / `content-gold-miner-public` / `video-editing-public` / `video-import-public` / `script-prep-public` group looks like one connected plugin suite for turning raw footage into edited reels via Descript — worth pulling into the repo together if you want the full pipeline documented.

> Update 2026-09-04: the "In this repo?" column above is now stale for these five — all were added. See §4.

---

## 3. Other dependencies worth flagging (not plugin-backed, but need something external)

These are your own `custom`-source skills already in the repo — no plugin involved, but each needs something outside Claude to actually run:

| Skill | Repo path | Dependency |
|---|---|---|
| `langsmith-fetch` | `skills/langsmith-fetch/` | Requires the `langsmith-fetch` CLI installed locally |
| `n8n+claude_Web/Desktop` (installed as `n8nclauddesktop`) | `skills/n8n+claudedesktop/` | A running n8n instance, Claude Desktop, `mcp-remote` installed, and an n8n MCP access token |
| `github-content-fetcher` | `skills/github-content-fetcher/` | GitHub access to the target repo, and write access to `Shri-Phnx/claude-skills` for the mirroring step |
| `semantic-scholar` (installed) / `aris-semantic-scholar` (repo) | `skills/aris-semantic-scholar/` | Semantic Scholar API access |
| `knowledge-base-health-check-skill` | `skills/knowledge-base-health-check-skill/` | An existing Claude-managed knowledge base to audit |

---

## 4. Update log

### 2026-09-04 — added the Descript reel-pipeline skills

Pulled all five plugin-backed skills flagged as "not in repo" in §1 straight from the live session's synced plugin files (`SKILL.md` only — none of the five ship extra reference files):

| Skill | New repo path | Source |
|---|---|---|
| `content-gold-miner-public` | `skills/content-gold-miner-public/SKILL.md` | `backingPluginId: plugin_018oaWKPEWqRtF7JBDVEqfkC` |
| `script-prep-public` | `skills/script-prep-public/SKILL.md` | `backingPluginId: plugin_017fkDowtHxBQYJg8EmAWZdf` |
| `video-editing-public` | `skills/video-editing-public/SKILL.md` | `backingPluginId: plugin_0191dqJSvd1QGfEFnNraxH3S` |
| `video-import-public` | `skills/video-import-public/SKILL.md` | `backingPluginId: plugin_01LzSmys6Wykj89qjpyHr2vi` |
| `watch` | `skills/watch/SKILL.md` | `backingPluginId: plugin_01FYaT54eZSb9nWN5sBJSm8y` |

These five form one pipeline: `video-import-public` gets footage into Descript → `content-gold-miner-public` (long raw footage) or `script-prep-public` (a single already-scripted piece) finds the keep-script → `video-editing-public` cuts and captions it via the Descript MCP. `watch` is separate — a general "watch and answer questions about a video" skill (yt-dlp + ffmpeg + captions/Whisper), not part of the Descript chain.

Dependencies carried over from §2 still apply — most notably, `video-editing-public` needs a **paid** Descript plan with AI credits to actually run.

§1 and §2 above are left as the original 2026-09-03 snapshot; this entry is the delta.

---

## Notes

- "Installed" here means visible in the live session's skill manifest at the time of this audit — session-specific, not a permanent record.
- A skill can appear installed under `custom` source even with no plugin behind it — those are skills you or someone added directly, not plugin-delivered.
- This file is additive: new sections should be appended or existing tables extended, not replaced, so past audit runs stay visible below as dated entries once this doc is updated again.
